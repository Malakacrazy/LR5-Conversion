namespace L5R.Engine.Cards.Scripts;

/// <summary>
/// scriptOverride for jade-tetsubo's action (the attachmentMyControlOnly restriction is
/// generic): while the attached character is participating, bow this attachment and
/// return all fate from a lower-military participating character to its owner. Needs the
/// attached character's status and skill as comparison references, beyond the closed
/// predicate vocabulary's source-only candidate model. Stubbed until the state model has
/// conflicts and attachments.
/// </summary>
public sealed class JadeTetsuboReturnFateFromLowerMilitaryParticipant : ICardScript
{
}
