namespace L5R.Engine.Cards.Scripts;

/// <summary>
/// scriptOverride for artisan-academy: a custom-duration lasting effect (showing and
/// allowing play of the top conflict deck card) that expires on any of three separate
/// events tied to the specific card captured when the action resolves. Stubbed until the
/// state model (task 8) has decks/lasting-effect durations to hook into.
/// </summary>
public sealed class ArtisanAcademyRevealTopCard : ICardScript
{
}
