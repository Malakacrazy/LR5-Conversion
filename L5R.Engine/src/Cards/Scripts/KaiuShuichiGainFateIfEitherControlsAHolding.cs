namespace L5R.Engine.Cards.Scripts;

/// <summary>
/// scriptOverride for kaiu-shuichi: action condition checks whether either player
/// controls a holding, a per-player dynamic count the current valueRef design (single
/// implicit subject) can't parameterize per side. Stubbed until dynamic values can name
/// their subject explicitly.
/// </summary>
public sealed class KaiuShuichiGainFateIfEitherControlsAHolding : ICardScript
{
}
